# Backend Environment Variables

# General
NODE_ENV=development # development | production | test
PORT=4000

# Interaction Mode (voice | text)
# Determines if the backend primarily uses voice (Deepgram/ElevenLabs) or text interfaces.
# Defaults to 'voice' if not set.
INTERACTION_MODE=voice

# MongoDB Connection String (replace with your actual URI)
MONGODB_URI=

# Redis Connection URL (replace with your actual URI, including credentials)
# Format: redis://[<user>][:<password>@]<host>[:<port>]
REDIS_URL=redis://localhost:6379

# Pinecone Credentials (replace with actual values when implemented)
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=

# Anthropic API Key (replace with actual key)
ANTHROPIC_API_KEY=

# ElevenLabs API Key (replace with actual key)
ELEVENLABS_API_KEY=

# Deepgram API Key (replace with actual key)
DEEPGRAM_API_KEY=

OPENAI_API_KEY=

# Datadog APM Configuration
DD_ENV=development
DD_SERVICE=ambi-backend
# DD_VERSION= # Optional: Set this to your app version (e.g., git commit hash)

# Datadog Agent Connection (Defaults usually work if agent is on localhost)
# DD_AGENT_HOST=localhost
# DD_TRACE_AGENT_PORT=8126

# Optional Datadog Features
# DD_LOGS_INJECTION=true # Enable if you want trace_id/span_id in logs
# DD_PROFILING_ENABLED=true # Enable Continuous Profiler
# DD_RUNTIME_METRICS_ENABLED=true # Enable Node.js runtime metrics